name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  placeholder:
    name: Placeholder CI scaffold
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Placeholder step
        run: echo "CI workflow scaffold for WinCC OA tests will be added here."
